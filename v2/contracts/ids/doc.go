// Package ids declares the identifier types used to name nodes, tablets,
// tables, requests, and transactions across GoMultiDB v2. These are plain
// value types (strings or fixed-size byte arrays); subsystems in engine,
// server, and gateway reference them to identify cluster members and
// in-flight work.
// This is scaffold-only; behavior is unimplemented.
package ids
